# Godot-specific ignores

# Import cache
.import/
# Export config and presets I guess?
export.cfg
export_presets.cfg

# Mono-specific ignores
.mono/
data_*/

# Binaries/builds
# TODO: maybe configure separate
# debug and release builds
bin/
build/
lib/

# Git addon
git_api*

# Assets
# TODO: decide what to do with them.
# Pretty sure I don't want them in the main code repo
assets/
